# Runtime dependencies for the Streamlit dashboard.
# (Local-only / data-prep dependencies live in dev/environment.yml.)
#
# deploy-kick nonce: 2026-06-13-4 (phase D+E — new content + transparency pages) — i18n foundation) — bump this comment to force Streamlit Cloud
# to fully rebuild + restart the app (dependency-file changes restart the
# process; plain code commits only hot-reload, which can leave stale modules
# in memory after merges that touch utils/).

# Core data
pandas>=2.2
numpy>=1.26
scipy>=1.13
statsmodels>=0.14
pyarrow>=15.0

# HTTP + parsing (used at runtime for any in-app fetching/parsing)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
pyyaml>=6.0       # curated YAML files (assets/curated/) read by the Athletics page

# Geospatial — used by map pages
geopandas>=1.0
shapely>=2.0
pyogrio>=0.8

# Web app + charts
streamlit>=1.36
plotly>=5.22
altair>=5.3

# PDF annual report generator (scripts/13_build_annual_report.py)
reportlab>=4.0
kaleido>=1.0
